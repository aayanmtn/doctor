import '../../../core/app_export.dart';/// This class is used in the [addpatient_item_widget] screen.
class AddpatientItemModel {AddpatientItemModel({this.addImage, this.id, }) { addImage = addImage  ?? ImageConstant.imgPlusIndigoA400;id = id  ?? ""; }

String? addImage;

String? id;

 }
